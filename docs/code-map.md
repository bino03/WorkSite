| **Frontend** | `components/budget/` — `BudgetExpensesDrawer` (despesas de uma rubrica: uma despesa **com fatura** abre o `InvoiceDetailDrawer` da lista de faturas, com reassociar via `BudgetItemPickerModal` e `IncidentDrawer`; só o gasto **sem fatura** abre o `BudgetExpenseDetailDrawer` — desde 2026-09-21), `BudgetExpenseFormDrawer`, # 🗺️ Mapa do código — onde vive cada funcionalidade
| **Frontend** | `components/budget/` — `BudgetExpensesDrawer` (despesas de uma rubrica: uma despesa **com fatura** abre o `InvoiceDetailDrawer` da lista de faturas, com reassociar via `BudgetItemPickerModal` e `IncidentDrawer`; só o gasto **sem fatura** abre o `BudgetExpenseDetailDrawer` — desde 2026-09-21), `BudgetExpenseFormDrawer`, 
| **Frontend** | `components/budget/` — `BudgetExpensesDrawer` (despesas de uma rubrica: uma despesa **com fatura** abre o `InvoiceDetailDrawer` da lista de faturas, com reassociar via `BudgetItemPickerModal` e `IncidentDrawer`; só o gasto **sem fatura** abre o `BudgetExpenseDetailDrawer` — desde 2026-09-21), `BudgetExpenseFormDrawer`, Este ficheiro responde a **"onde está o código disto?"**. Não explica como funciona nem porquê —
| **Frontend** | `components/budget/` — `BudgetExpensesDrawer` (despesas de uma rubrica: uma despesa **com fatura** abre o `InvoiceDetailDrawer` da lista de faturas, com reassociar via `BudgetItemPickerModal` e `IncidentDrawer`; só o gasto **sem fatura** abre o `BudgetExpenseDetailDrawer` — desde 2026-09-21), `BudgetExpenseFormDrawer`, para isso há os outros:
| **Frontend** | `components/budget/` — `BudgetExpensesDrawer` (despesas de uma rubrica: uma despesa **com fatura** abre o `InvoiceDetailDrawer` da lista de faturas, com reassociar via `BudgetItemPickerModal` e `IncidentDrawer`; só o gasto **sem fatura** abre o `BudgetExpenseDetailDrawer` — desde 2026-09-21), `BudgetExpenseFormDrawer`, 
| **Frontend** | `components/budget/` — `BudgetExpensesDrawer` (despesas de uma rubrica: uma despesa **com fatura** abre o `InvoiceDetailDrawer` da lista de faturas, com reassociar via `BudgetItemPickerModal` e `IncidentDrawer`; só o gasto **sem fatura** abre o `BudgetExpenseDetailDrawer` — desde 2026-09-21), `BudgetExpenseFormDrawer`, | Pergunta | Onde |
| **Frontend** | `components/budget/` — `BudgetExpensesDrawer` (despesas de uma rubrica: uma despesa **com fatura** abre o `InvoiceDetailDrawer` da lista de faturas, com reassociar via `BudgetItemPickerModal` e `IncidentDrawer`; só o gasto **sem fatura** abre o `BudgetExpenseDetailDrawer` — desde 2026-09-21), `BudgetExpenseFormDrawer`, |---|---|
| **Frontend** | `components/budget/` — `BudgetExpensesDrawer` (despesas de uma rubrica: uma despesa **com fatura** abre o `InvoiceDetailDrawer` da lista de faturas, com reassociar via `BudgetItemPickerModal` e `IncidentDrawer`; só o gasto **sem fatura** abre o `BudgetExpenseDetailDrawer` — desde 2026-09-21), `BudgetExpenseFormDrawer`, | **Onde está?** | este ficheiro |
| **Frontend** | `components/budget/` — `BudgetExpensesDrawer` (despesas de uma rubrica: uma despesa **com fatura** abre o `InvoiceDetailDrawer` da lista de faturas, com reassociar via `BudgetItemPickerModal` e `IncidentDrawer`; só o gasto **sem fatura** abre o `BudgetExpenseDetailDrawer` — desde 2026-09-21), `BudgetExpenseFormDrawer`, | Que rotas e regras de acesso tem? | [[api.md]] |
| **Frontend** | `components/budget/` — `BudgetExpensesDrawer` (despesas de uma rubrica: uma despesa **com fatura** abre o `InvoiceDetailDrawer` da lista de faturas, com reassociar via `BudgetItemPickerModal` e `IncidentDrawer`; só o gasto **sem fatura** abre o `BudgetExpenseDetailDrawer` — desde 2026-09-21), `BudgetExpenseFormDrawer`, | Que tabelas e colunas? | [[database.md]] |
| **Frontend** | `components/budget/` — `BudgetExpensesDrawer` (despesas de uma rubrica: uma despesa **com fatura** abre o `InvoiceDetailDrawer` da lista de faturas, com reassociar via `BudgetItemPickerModal` e `IncidentDrawer`; só o gasto **sem fatura** abre o `BudgetExpenseDetailDrawer` — desde 2026-09-21), `BudgetExpenseFormDrawer`, | Como se faz uma alteração aqui? | [[skills/SKILLS-INDEX]] (skills) · [[backend-conventions]] · [[skills/references/frontend-visual-consistency]] |
| **Frontend** | `components/budget/` — `BudgetExpensesDrawer` (despesas de uma rubrica: uma despesa **com fatura** abre o `InvoiceDetailDrawer` da lista de faturas, com reassociar via `BudgetItemPickerModal` e `IncidentDrawer`; só o gasto **sem fatura** abre o `BudgetExpenseDetailDrawer` — desde 2026-09-21), `BudgetExpenseFormDrawer`, | Como se chama isto? | [[skills/references/project-vocabulary]] |
| **Frontend** | `components/budget/` — `BudgetExpensesDrawer` (despesas de uma rubrica: uma despesa **com fatura** abre o `InvoiceDetailDrawer` da lista de faturas, com reassociar via `BudgetItemPickerModal` e `IncidentDrawer`; só o gasto **sem fatura** abre o `BudgetExpenseDetailDrawer` — desde 2026-09-21), `BudgetExpenseFormDrawer`, 
| **Frontend** | `components/budget/` — `BudgetExpensesDrawer` (despesas de uma rubrica: uma despesa **com fatura** abre o `InvoiceDetailDrawer` da lista de faturas, com reassociar via `BudgetItemPickerModal` e `IncidentDrawer`; só o gasto **sem fatura** abre o `BudgetExpenseDetailDrawer` — desde 2026-09-21), `BudgetExpenseFormDrawer`, > Mantém-se **grosso de propósito**: portas de entrada e ficheiros-chave, não listas
| **Frontend** | `components/budget/` — `BudgetExpensesDrawer` (despesas de uma rubrica: uma despesa **com fatura** abre o `InvoiceDetailDrawer` da lista de faturas, com reassociar via `BudgetItemPickerModal` e `IncidentDrawer`; só o gasto **sem fatura** abre o `BudgetExpenseDetailDrawer` — desde 2026-09-21), `BudgetExpenseFormDrawer`, > exaustivas. Um mapa que tenta listar tudo fica errado à primeira semana.
| **Frontend** | `components/budget/` — `BudgetExpensesDrawer` (despesas de uma rubrica: uma despesa **com fatura** abre o `InvoiceDetailDrawer` da lista de faturas, com reassociar via `BudgetItemPickerModal` e `IncidentDrawer`; só o gasto **sem fatura** abre o `BudgetExpenseDetailDrawer` — desde 2026-09-21), `BudgetExpenseFormDrawer`, 
| **Frontend** | `components/budget/` — `BudgetExpensesDrawer` (despesas de uma rubrica: uma despesa **com fatura** abre o `InvoiceDetailDrawer` da lista de faturas, com reassociar via `BudgetItemPickerModal` e `IncidentDrawer`; só o gasto **sem fatura** abre o `BudgetExpenseDetailDrawer` — desde 2026-09-21), `BudgetExpenseFormDrawer`, ---
| **Frontend** | `components/budget/` — `BudgetExpensesDrawer` (despesas de uma rubrica: uma despesa **com fatura** abre o `InvoiceDetailDrawer` da lista de faturas, com reassociar via `BudgetItemPickerModal` e `IncidentDrawer`; só o gasto **sem fatura** abre o `BudgetExpenseDetailDrawer` — desde 2026-09-21), `BudgetExpenseFormDrawer`, 
| **Frontend** | `components/budget/` — `BudgetExpensesDrawer` (despesas de uma rubrica: uma despesa **com fatura** abre o `InvoiceDetailDrawer` da lista de faturas, com reassociar via `BudgetItemPickerModal` e `IncidentDrawer`; só o gasto **sem fatura** abre o `BudgetExpenseDetailDrawer` — desde 2026-09-21), `BudgetExpenseFormDrawer`, ## Faturas de obra
| **Frontend** | `components/budget/` — `BudgetExpensesDrawer` (despesas de uma rubrica: uma despesa **com fatura** abre o `InvoiceDetailDrawer` da lista de faturas, com reassociar via `BudgetItemPickerModal` e `IncidentDrawer`; só o gasto **sem fatura** abre o `BudgetExpenseDetailDrawer` — desde 2026-09-21), `BudgetExpenseFormDrawer`, 
| **Frontend** | `components/budget/` — `BudgetExpensesDrawer` (despesas de uma rubrica: uma despesa **com fatura** abre o `InvoiceDetailDrawer` da lista de faturas, com reassociar via `BudgetItemPickerModal` e `IncidentDrawer`; só o gasto **sem fatura** abre o `BudgetExpenseDetailDrawer` — desde 2026-09-21), `BudgetExpenseFormDrawer`, O documento que vem da obra: carregar, ler o QR da AT, corrigir à mão, classificar em rubricas.
| **Frontend** | `components/budget/` — `BudgetExpensesDrawer` (despesas de uma rubrica: uma despesa **com fatura** abre o `InvoiceDetailDrawer` da lista de faturas, com reassociar via `BudgetItemPickerModal` e `IncidentDrawer`; só o gasto **sem fatura** abre o `BudgetExpenseDetailDrawer` — desde 2026-09-21), `BudgetExpenseFormDrawer`, Uma fatura tem um **âmbito** (`scope`, `V26`): `PROJECT` (de uma obra), `COMPANY` (despesa da
| **Frontend** | `components/budget/` — `BudgetExpensesDrawer` (despesas de uma rubrica: uma despesa **com fatura** abre o `InvoiceDetailDrawer` da lista de faturas, com reassociar via `BudgetItemPickerModal` e `IncidentDrawer`; só o gasto **sem fatura** abre o `BudgetExpenseDetailDrawer` — desde 2026-09-21), `BudgetExpenseFormDrawer`, empresa, sem obra) ou `UNIDENTIFIED` (quarentena — ainda não se sabe de quem é).
| **Frontend** | `components/budget/` — `BudgetExpensesDrawer` (despesas de uma rubrica: uma despesa **com fatura** abre o `InvoiceDetailDrawer` da lista de faturas, com reassociar via `BudgetItemPickerModal` e `IncidentDrawer`; só o gasto **sem fatura** abre o `BudgetExpenseDetailDrawer` — desde 2026-09-21), `BudgetExpenseFormDrawer`, 
| **Frontend** | `components/budget/` — `BudgetExpensesDrawer` (despesas de uma rubrica: uma despesa **com fatura** abre o `InvoiceDetailDrawer` da lista de faturas, com reassociar via `BudgetItemPickerModal` e `IncidentDrawer`; só o gasto **sem fatura** abre o `BudgetExpenseDetailDrawer` — desde 2026-09-21), `BudgetExpenseFormDrawer`, | Camada | Ficheiros |
| **Frontend** | `components/budget/` — `BudgetExpensesDrawer` (despesas de uma rubrica: uma despesa **com fatura** abre o `InvoiceDetailDrawer` da lista de faturas, com reassociar via `BudgetItemPickerModal` e `IncidentDrawer`; só o gasto **sem fatura** abre o `BudgetExpenseDetailDrawer` — desde 2026-09-21), `BudgetExpenseFormDrawer`, |---|---|
| **Frontend** | `components/budget/` — `BudgetExpensesDrawer` (despesas de uma rubrica: uma despesa **com fatura** abre o `InvoiceDetailDrawer` da lista de faturas, com reassociar via `BudgetItemPickerModal` e `IncidentDrawer`; só o gasto **sem fatura** abre o `BudgetExpenseDetailDrawer` — desde 2026-09-21), `BudgetExpenseFormDrawer`, | **Entrada** | rota `/backoffice/empreendimentos/:enterpriseId/invoices` → `pages/backoffice/enterprise/EnterpriseInvoicesPage.tsx` |
| **Frontend** | `components/budget/` — `BudgetExpensesDrawer` (despesas de uma rubrica: uma despesa **com fatura** abre o `InvoiceDetailDrawer` da lista de faturas, com reassociar via `BudgetItemPickerModal` e `IncidentDrawer`; só o gasto **sem fatura** abre o `BudgetExpenseDetailDrawer` — desde 2026-09-21), `BudgetExpenseFormDrawer`, | **Classificar** | rota `/backoffice/empreendimentos/:enterpriseId/classify` → `pages/backoffice/enterprise/ClassifyInvoicesPage.tsx` (botão "Classificar" na página das faturas): repartir a fatura por rubricas (`V32`) · `components/invoices/RubricSearchField.tsx` · sugestão em `GET /construction-invoices/{id}/rubric-suggestion` |
| **Frontend** | `components/budget/` — `BudgetExpensesDrawer` (despesas de uma rubrica: uma despesa **com fatura** abre o `InvoiceDetailDrawer` da lista de faturas, com reassociar via `BudgetItemPickerModal` e `IncidentDrawer`; só o gasto **sem fatura** abre o `BudgetExpenseDetailDrawer` — desde 2026-09-21), `BudgetExpenseFormDrawer`, | **Fora das obras** | menu lateral, grupo **Faturas** (`layouts/AppLayout.tsx`): "Por identificar" → `/backoffice/invoices/unidentified` (`pages/backoffice/invoices/UnidentifiedInvoicesPage.tsx`) · "Despesas da empresa" → `/backoffice/invoices/company` (`CompanyInvoicesPage.tsx`) — as duas são o mesmo `ScopedInvoicesPage.tsx` com `scope` diferente, a ler `GET /construction-invoices/unidentified` e `/company` |
| **Frontend** | `components/budget/` — `BudgetExpensesDrawer` (despesas de uma rubrica: uma despesa **com fatura** abre o `InvoiceDetailDrawer` da lista de faturas, com reassociar via `BudgetItemPickerModal` e `IncidentDrawer`; só o gasto **sem fatura** abre o `BudgetExpenseDetailDrawer` — desde 2026-09-21), `BudgetExpenseFormDrawer`, | **Frontend** | `components/invoices/` — `InvoicesList`, `InvoiceFiltersModal` (pesquisa avançada: modal centrado, ícone de filtro com contagem + "Limpar filtros" na página, sem chips — 2026-09-21; helpers em `invoiceFilters.ts`), `OutstandingTotalBadge`, `InvoiceUploadDrawer` (2 fases), `InvoiceRegisterDrawer` (sem ficheiro), `InvoiceDetailDrawer` (correção manual, líquido e NC ligadas), `InvoiceDocumentGallery`, `CreditNoteDrawer` (fase 3), `BudgetItemPickerModal`, `invoiceNumber.ts` (tipo + série), `invoiceFormSchema.ts`/`creditNoteFormSchema.ts` · `components/construction/InvoicePreviewModal.tsx` · `services/invoiceService.ts` · `types/invoice.ts` |
| **Frontend** | `components/budget/` — `BudgetExpensesDrawer` (despesas de uma rubrica: uma despesa **com fatura** abre o `InvoiceDetailDrawer` da lista de faturas, com reassociar via `BudgetItemPickerModal` e `IncidentDrawer`; só o gasto **sem fatura** abre o `BudgetExpenseDetailDrawer` — desde 2026-09-21), `BudgetExpenseFormDrawer`, | **Backend** | `enterprises/controller/ConstructionInvoiceController` · `service/ConstructionInvoiceService` (o núcleo — upload, duplicados, correção, notas de crédito, repartição, transferência) · `AtInvoiceQrService` + `WeChatQrCodeService` (leitura do QR) · `InvoiceThumbnailService` · `InvoiceCompressionService` · `DespesasExcelImportService` (fase 6) · `repository/ConstructionInvoiceRepository` |
| **Frontend** | `components/budget/` — `BudgetExpensesDrawer` (despesas de uma rubrica: uma despesa **com fatura** abre o `InvoiceDetailDrawer` da lista de faturas, com reassociar via `BudgetItemPickerModal` e `IncidentDrawer`; só o gasto **sem fatura** abre o `BudgetExpenseDetailDrawer` — desde 2026-09-21), `BudgetExpenseFormDrawer`, | **Base de dados** | `worksite.construction_invoice` — `V16`, `V17` (ATCUD único), `V18` (checksum), `V26` (`scope`), `V29` (unicidade global) · `construction_invoice_document` (o ficheiro deixa de viver na fatura) — `V24`, `V25`, `V27` (estado), `V28` (tipo) · repartição por várias rubricas — `V32` |
| **Frontend** | `components/budget/` — `BudgetExpensesDrawer` (despesas de uma rubrica: uma despesa **com fatura** abre o `InvoiceDetailDrawer` da lista de faturas, com reassociar via `BudgetItemPickerModal` e `IncidentDrawer`; só o gasto **sem fatura** abre o `BudgetExpenseDetailDrawer` — desde 2026-09-21), `BudgetExpenseFormDrawer`, | **Detalhe** | [[api.md]] → "Faturas de obra", "Notas de crédito", "Classificar faturas em rubricas" · [[database.md]] · [[faturas-modelo-alvo]] |
| **Frontend** | `components/budget/` — `BudgetExpensesDrawer` (despesas de uma rubrica: uma despesa **com fatura** abre o `InvoiceDetailDrawer` da lista de faturas, com reassociar via `BudgetItemPickerModal` e `IncidentDrawer`; só o gasto **sem fatura** abre o `BudgetExpenseDetailDrawer` — desde 2026-09-21), `BudgetExpenseFormDrawer`, 
| **Frontend** | `components/budget/` — `BudgetExpensesDrawer` (despesas de uma rubrica: uma despesa **com fatura** abre o `InvoiceDetailDrawer` da lista de faturas, com reassociar via `BudgetItemPickerModal` e `IncidentDrawer`; só o gasto **sem fatura** abre o `BudgetExpenseDetailDrawer` — desde 2026-09-21), `BudgetExpenseFormDrawer`, ## Pagamentos
| **Frontend** | `components/budget/` — `BudgetExpensesDrawer` (despesas de uma rubrica: uma despesa **com fatura** abre o `InvoiceDetailDrawer` da lista de faturas, com reassociar via `BudgetItemPickerModal` e `IncidentDrawer`; só o gasto **sem fatura** abre o `BudgetExpenseDetailDrawer` — desde 2026-09-21), `BudgetExpenseFormDrawer`, 
| **Frontend** | `components/budget/` — `BudgetExpensesDrawer` (despesas de uma rubrica: uma despesa **com fatura** abre o `InvoiceDetailDrawer` da lista de faturas, com reassociar via `BudgetItemPickerModal` e `IncidentDrawer`; só o gasto **sem fatura** abre o `BudgetExpenseDetailDrawer` — desde 2026-09-21), `BudgetExpenseFormDrawer`, Fase 2 da paridade com o Excel: "dar como pago" é um movimento (`payment`) ligado a uma ou
| **Frontend** | `components/budget/` — `BudgetExpensesDrawer` (despesas de uma rubrica: uma despesa **com fatura** abre o `InvoiceDetailDrawer` da lista de faturas, com reassociar via `BudgetItemPickerModal` e `IncidentDrawer`; só o gasto **sem fatura** abre o `BudgetExpenseDetailDrawer` — desde 2026-09-21), `BudgetExpenseFormDrawer`, várias faturas (`invoice_payment`) — o caso normal e o pagamento agregado são o mesmo modelo.
| **Frontend** | `components/budget/` — `BudgetExpensesDrawer` (despesas de uma rubrica: uma despesa **com fatura** abre o `InvoiceDetailDrawer` da lista de faturas, com reassociar via `BudgetItemPickerModal` e `IncidentDrawer`; só o gasto **sem fatura** abre o `BudgetExpenseDetailDrawer` — desde 2026-09-21), `BudgetExpenseFormDrawer`, 
| **Frontend** | `components/budget/` — `BudgetExpensesDrawer` (despesas de uma rubrica: uma despesa **com fatura** abre o `InvoiceDetailDrawer` da lista de faturas, com reassociar via `BudgetItemPickerModal` e `IncidentDrawer`; só o gasto **sem fatura** abre o `BudgetExpenseDetailDrawer` — desde 2026-09-21), `BudgetExpenseFormDrawer`, | Camada | Ficheiros |
| **Frontend** | `components/budget/` — `BudgetExpensesDrawer` (despesas de uma rubrica: uma despesa **com fatura** abre o `InvoiceDetailDrawer` da lista de faturas, com reassociar via `BudgetItemPickerModal` e `IncidentDrawer`; só o gasto **sem fatura** abre o `BudgetExpenseDetailDrawer` — desde 2026-09-21), `BudgetExpenseFormDrawer`, |---|---|
| **Frontend** | `components/budget/` — `BudgetExpensesDrawer` (despesas de uma rubrica: uma despesa **com fatura** abre o `InvoiceDetailDrawer` da lista de faturas, com reassociar via `BudgetItemPickerModal` e `IncidentDrawer`; só o gasto **sem fatura** abre o `BudgetExpenseDetailDrawer` — desde 2026-09-21), `BudgetExpenseFormDrawer`, | **Entrada** | "Marcar como paga" no `InvoiceDetailDrawer` (uma fatura) · "Registar pagamento" na barra de seleção de `EnterpriseInvoicesPage` (várias faturas, um movimento) |
| **Frontend** | `components/budget/` — `BudgetExpensesDrawer` (despesas de uma rubrica: uma despesa **com fatura** abre o `InvoiceDetailDrawer` da lista de faturas, com reassociar via `BudgetItemPickerModal` e `IncidentDrawer`; só o gasto **sem fatura** abre o `BudgetExpenseDetailDrawer` — desde 2026-09-21), `BudgetExpenseFormDrawer`, | **Frontend** | `components/invoices/MarkPaidDrawer.tsx` + `AggregatePaymentDrawer.tsx` · `paymentFormSchema.ts` · `services/paymentService.ts` · os campos `paymentStatus`/`payments` em `types/invoice.ts` |
| **Frontend** | `components/budget/` — `BudgetExpensesDrawer` (despesas de uma rubrica: uma despesa **com fatura** abre o `InvoiceDetailDrawer` da lista de faturas, com reassociar via `BudgetItemPickerModal` e `IncidentDrawer`; só o gasto **sem fatura** abre o `BudgetExpenseDetailDrawer` — desde 2026-09-21), `BudgetExpenseFormDrawer`, | **Backend** | `enterprises/controller/PaymentController` (`POST /construction-invoices/{id}/payments`, `POST /construction-invoices/payments`, `DELETE /construction-invoices/payments/{paymentId}`) · `service/PaymentService` · `model/Payment` + `InvoicePayment` (+ `InvoicePaymentId`) · `enums/PaymentMethod`, `PaymentStatus` · `dto/payment/` · `repository/PaymentRepository` + `InvoicePaymentRepository` |
| **Frontend** | `components/budget/` — `BudgetExpensesDrawer` (despesas de uma rubrica: uma despesa **com fatura** abre o `InvoiceDetailDrawer` da lista de faturas, com reassociar via `BudgetItemPickerModal` e `IncidentDrawer`; só o gasto **sem fatura** abre o `BudgetExpenseDetailDrawer` — desde 2026-09-21), `BudgetExpenseFormDrawer`, | **Base de dados** | `worksite.payment` + `invoice_payment` — `V31` |
| **Frontend** | `components/budget/` — `BudgetExpensesDrawer` (despesas de uma rubrica: uma despesa **com fatura** abre o `InvoiceDetailDrawer` da lista de faturas, com reassociar via `BudgetItemPickerModal` e `IncidentDrawer`; só o gasto **sem fatura** abre o `BudgetExpenseDetailDrawer` — desde 2026-09-21), `BudgetExpenseFormDrawer`, | **Detalhe** | [[api.md]] → "Pagamentos" · [[faturas-modelo-alvo]] §2.3 |
| **Frontend** | `components/budget/` — `BudgetExpensesDrawer` (despesas de uma rubrica: uma despesa **com fatura** abre o `InvoiceDetailDrawer` da lista de faturas, com reassociar via `BudgetItemPickerModal` e `IncidentDrawer`; só o gasto **sem fatura** abre o `BudgetExpenseDetailDrawer` — desde 2026-09-21), `BudgetExpenseFormDrawer`, 
| **Frontend** | `components/budget/` — `BudgetExpensesDrawer` (despesas de uma rubrica: uma despesa **com fatura** abre o `InvoiceDetailDrawer` da lista de faturas, com reassociar via `BudgetItemPickerModal` e `IncidentDrawer`; só o gasto **sem fatura** abre o `BudgetExpenseDetailDrawer` — desde 2026-09-21), `BudgetExpenseFormDrawer`, ## Inconsistências & transferências
| **Frontend** | `components/budget/` — `BudgetExpensesDrawer` (despesas de uma rubrica: uma despesa **com fatura** abre o `InvoiceDetailDrawer` da lista de faturas, com reassociar via `BudgetItemPickerModal` e `IncidentDrawer`; só o gasto **sem fatura** abre o `BudgetExpenseDetailDrawer` — desde 2026-09-21), `BudgetExpenseFormDrawer`, 
| **Frontend** | `components/budget/` — `BudgetExpensesDrawer` (despesas de uma rubrica: uma despesa **com fatura** abre o `InvoiceDetailDrawer` da lista de faturas, com reassociar via `BudgetItemPickerModal` e `IncidentDrawer`; só o gasto **sem fatura** abre o `BudgetExpenseDetailDrawer` — desde 2026-09-21), `BudgetExpenseFormDrawer`, Fase 5. Mudar uma fatura de obra ou de âmbito é uma **transferência com razão** (nunca edição do
| **Frontend** | `components/budget/` — `BudgetExpensesDrawer` (despesas de uma rubrica: uma despesa **com fatura** abre o `InvoiceDetailDrawer` da lista de faturas, com reassociar via `BudgetItemPickerModal` e `IncidentDrawer`; só o gasto **sem fatura** abre o `BudgetExpenseDetailDrawer` — desde 2026-09-21), `BudgetExpenseFormDrawer`, `scope`); o que ficou por conciliar depois disso vira uma **inconsistência** — nota livre sobre
| **Frontend** | `components/budget/` — `BudgetExpensesDrawer` (despesas de uma rubrica: uma despesa **com fatura** abre o `InvoiceDetailDrawer` da lista de faturas, com reassociar via `BudgetItemPickerModal` e `IncidentDrawer`; só o gasto **sem fatura** abre o `BudgetExpenseDetailDrawer` — desde 2026-09-21), `BudgetExpenseFormDrawer`, uma ou mais faturas, aberta à mão, resolvida à mão.
| **Frontend** | `components/budget/` — `BudgetExpensesDrawer` (despesas de uma rubrica: uma despesa **com fatura** abre o `InvoiceDetailDrawer` da lista de faturas, com reassociar via `BudgetItemPickerModal` e `IncidentDrawer`; só o gasto **sem fatura** abre o `BudgetExpenseDetailDrawer` — desde 2026-09-21), `BudgetExpenseFormDrawer`, 
| **Frontend** | `components/budget/` — `BudgetExpensesDrawer` (despesas de uma rubrica: uma despesa **com fatura** abre o `InvoiceDetailDrawer` da lista de faturas, com reassociar via `BudgetItemPickerModal` e `IncidentDrawer`; só o gasto **sem fatura** abre o `BudgetExpenseDetailDrawer` — desde 2026-09-21), `BudgetExpenseFormDrawer`, | Camada | Ficheiros |
| **Frontend** | `components/budget/` — `BudgetExpensesDrawer` (despesas de uma rubrica: uma despesa **com fatura** abre o `InvoiceDetailDrawer` da lista de faturas, com reassociar via `BudgetItemPickerModal` e `IncidentDrawer`; só o gasto **sem fatura** abre o `BudgetExpenseDetailDrawer` — desde 2026-09-21), `BudgetExpenseFormDrawer`, |---|---|
| **Frontend** | `components/budget/` — `BudgetExpensesDrawer` (despesas de uma rubrica: uma despesa **com fatura** abre o `InvoiceDetailDrawer` da lista de faturas, com reassociar via `BudgetItemPickerModal` e `IncidentDrawer`; só o gasto **sem fatura** abre o `BudgetExpenseDetailDrawer` — desde 2026-09-21), `BudgetExpenseFormDrawer`, | **Entrada** | menu lateral, grupo **Faturas** → "Inconsistências" → `/backoffice/invoices/incidents` (`pages/backoffice/invoices/InvoiceIncidentsPage.tsx`) · a ação "Transferir" no detalhe de uma fatura |
| **Frontend** | `components/budget/` — `BudgetExpensesDrawer` (despesas de uma rubrica: uma despesa **com fatura** abre o `InvoiceDetailDrawer` da lista de faturas, com reassociar via `BudgetItemPickerModal` e `IncidentDrawer`; só o gasto **sem fatura** abre o `BudgetExpenseDetailDrawer` — desde 2026-09-21), `BudgetExpenseFormDrawer`, | **Frontend** | `components/invoices/TransferInvoiceDrawer.tsx` + `transferFormSchema.ts` (depois de transferir, sugere abrir uma inconsistência) · `IncidentDrawer.tsx` + `incidentFormSchema.ts` + `toIncidentInvoiceRef.ts` · `services/incidentService.ts` · `types/incident.ts` |
| **Frontend** | `components/budget/` — `BudgetExpensesDrawer` (despesas de uma rubrica: uma despesa **com fatura** abre o `InvoiceDetailDrawer` da lista de faturas, com reassociar via `BudgetItemPickerModal` e `IncidentDrawer`; só o gasto **sem fatura** abre o `BudgetExpenseDetailDrawer` — desde 2026-09-21), `BudgetExpenseFormDrawer`, | **Backend** | transferência: `ConstructionInvoiceService` via `POST /construction-invoices/{id}/transfer` (`dto/invoice/request/InvoiceTransferDTO`, `response/InvoiceTransferResultDTO`) · inconsistências: `enterprises/controller/InvoiceIncidentController` (`/invoice-incidents`) · `service/InvoiceIncidentService` · `model/InvoiceIncident` · `dto/incident/` · `repository/InvoiceIncidentRepository` |
| **Frontend** | `components/budget/` — `BudgetExpensesDrawer` (despesas de uma rubrica: uma despesa **com fatura** abre o `InvoiceDetailDrawer` da lista de faturas, com reassociar via `BudgetItemPickerModal` e `IncidentDrawer`; só o gasto **sem fatura** abre o `BudgetExpenseDetailDrawer` — desde 2026-09-21), `BudgetExpenseFormDrawer`, | **Base de dados** | `worksite.invoice_incident` + `invoice_incident_invoice` — `V33` · o check `ck_invoice_scope_enterprise` de `V26` é o que impede uma fatura `COMPANY`/`UNIDENTIFIED` de ficar com obra |
| **Frontend** | `components/budget/` — `BudgetExpensesDrawer` (despesas de uma rubrica: uma despesa **com fatura** abre o `InvoiceDetailDrawer` da lista de faturas, com reassociar via `BudgetItemPickerModal` e `IncidentDrawer`; só o gasto **sem fatura** abre o `BudgetExpenseDetailDrawer` — desde 2026-09-21), `BudgetExpenseFormDrawer`, | **Detalhe** | [[api.md]] → "Transferir faturas", "Inconsistências" · [[faturas-modelo-alvo]] §4 |
| **Frontend** | `components/budget/` — `BudgetExpensesDrawer` (despesas de uma rubrica: uma despesa **com fatura** abre o `InvoiceDetailDrawer` da lista de faturas, com reassociar via `BudgetItemPickerModal` e `IncidentDrawer`; só o gasto **sem fatura** abre o `BudgetExpenseDetailDrawer` — desde 2026-09-21), `BudgetExpenseFormDrawer`, 
| **Frontend** | `components/budget/` — `BudgetExpensesDrawer` (despesas de uma rubrica: uma despesa **com fatura** abre o `InvoiceDetailDrawer` da lista de faturas, com reassociar via `BudgetItemPickerModal` e `IncidentDrawer`; só o gasto **sem fatura** abre o `BudgetExpenseDetailDrawer` — desde 2026-09-21), `BudgetExpenseFormDrawer`, ## Fornecedores
| **Frontend** | `components/budget/` — `BudgetExpensesDrawer` (despesas de uma rubrica: uma despesa **com fatura** abre o `InvoiceDetailDrawer` da lista de faturas, com reassociar via `BudgetItemPickerModal` e `IncidentDrawer`; só o gasto **sem fatura** abre o `BudgetExpenseDetailDrawer` — desde 2026-09-21), `BudgetExpenseFormDrawer`, 
| **Frontend** | `components/budget/` — `BudgetExpensesDrawer` (despesas de uma rubrica: uma despesa **com fatura** abre o `InvoiceDetailDrawer` da lista de faturas, com reassociar via `BudgetItemPickerModal` e `IncidentDrawer`; só o gasto **sem fatura** abre o `BudgetExpenseDetailDrawer` — desde 2026-09-21), `BudgetExpenseFormDrawer`, Catálogo NIF → nome da empresa. Existe porque o QR da AT identifica o emitente só pelo NIF.
| **Frontend** | `components/budget/` — `BudgetExpensesDrawer` (despesas de uma rubrica: uma despesa **com fatura** abre o `InvoiceDetailDrawer` da lista de faturas, com reassociar via `BudgetItemPickerModal` e `IncidentDrawer`; só o gasto **sem fatura** abre o `BudgetExpenseDetailDrawer` — desde 2026-09-21), `BudgetExpenseFormDrawer`, 
| **Frontend** | `components/budget/` — `BudgetExpensesDrawer` (despesas de uma rubrica: uma despesa **com fatura** abre o `InvoiceDetailDrawer` da lista de faturas, com reassociar via `BudgetItemPickerModal` e `IncidentDrawer`; só o gasto **sem fatura** abre o `BudgetExpenseDetailDrawer` — desde 2026-09-21), `BudgetExpenseFormDrawer`, | Camada | Ficheiros |
| **Frontend** | `components/budget/` — `BudgetExpensesDrawer` (despesas de uma rubrica: uma despesa **com fatura** abre o `InvoiceDetailDrawer` da lista de faturas, com reassociar via `BudgetItemPickerModal` e `IncidentDrawer`; só o gasto **sem fatura** abre o `BudgetExpenseDetailDrawer` — desde 2026-09-21), `BudgetExpenseFormDrawer`, |---|---|
| **Frontend** | `components/budget/` — `BudgetExpensesDrawer` (despesas de uma rubrica: uma despesa **com fatura** abre o `InvoiceDetailDrawer` da lista de faturas, com reassociar via `BudgetItemPickerModal` e `IncidentDrawer`; só o gasto **sem fatura** abre o `BudgetExpenseDetailDrawer` — desde 2026-09-21), `BudgetExpenseFormDrawer`, | **Entrada** | ⚙️ **Definições** no cabeçalho (`layouts/AppLayout.tsx`) → `components/suppliers/SuppliersDrawer.tsx` |
| **Frontend** | `components/budget/` — `BudgetExpensesDrawer` (despesas de uma rubrica: uma despesa **com fatura** abre o `InvoiceDetailDrawer` da lista de faturas, com reassociar via `BudgetItemPickerModal` e `IncidentDrawer`; só o gasto **sem fatura** abre o `BudgetExpenseDetailDrawer` — desde 2026-09-21), `BudgetExpenseFormDrawer`, | **Frontend** | `services/supplierService.ts` · `types/supplier.ts` · evento `SUPPLIERS_CHANGED_EVENT` ouvido por `EnterpriseInvoicesPage` |
| **Frontend** | `components/budget/` — `BudgetExpensesDrawer` (despesas de uma rubrica: uma despesa **com fatura** abre o `InvoiceDetailDrawer` da lista de faturas, com reassociar via `BudgetItemPickerModal` e `IncidentDrawer`; só o gasto **sem fatura** abre o `BudgetExpenseDetailDrawer` — desde 2026-09-21), `BudgetExpenseFormDrawer`, | **Backend** | `enterprises/controller/SupplierController` · `service/SupplierService` · `repository/SupplierRepository` · `model/Supplier` · `mapper/SupplierMapper` · as três queries de agregação em `ConstructionInvoiceRepository` |
| **Frontend** | `components/budget/` — `BudgetExpensesDrawer` (despesas de uma rubrica: uma despesa **com fatura** abre o `InvoiceDetailDrawer` da lista de faturas, com reassociar via `BudgetItemPickerModal` e `IncidentDrawer`; só o gasto **sem fatura** abre o `BudgetExpenseDetailDrawer` — desde 2026-09-21), `BudgetExpenseFormDrawer`, | **Base de dados** | `worksite.supplier` — `V19` |
| **Frontend** | `components/budget/` — `BudgetExpensesDrawer` (despesas de uma rubrica: uma despesa **com fatura** abre o `InvoiceDetailDrawer` da lista de faturas, com reassociar via `BudgetItemPickerModal` e `IncidentDrawer`; só o gasto **sem fatura** abre o `BudgetExpenseDetailDrawer` — desde 2026-09-21), `BudgetExpenseFormDrawer`, | **Detalhe** | [[api.md]] → "Fornecedores" |
| **Frontend** | `components/budget/` — `BudgetExpensesDrawer` (despesas de uma rubrica: uma despesa **com fatura** abre o `InvoiceDetailDrawer` da lista de faturas, com reassociar via `BudgetItemPickerModal` e `IncidentDrawer`; só o gasto **sem fatura** abre o `BudgetExpenseDetailDrawer` — desde 2026-09-21), `BudgetExpenseFormDrawer`, 
| **Frontend** | `components/budget/` — `BudgetExpensesDrawer` (despesas de uma rubrica: uma despesa **com fatura** abre o `InvoiceDetailDrawer` da lista de faturas, com reassociar via `BudgetItemPickerModal` e `IncidentDrawer`; só o gasto **sem fatura** abre o `BudgetExpenseDetailDrawer` — desde 2026-09-21), `BudgetExpenseFormDrawer`, ## Provedores de email (SMTP)
| **Frontend** | `components/budget/` — `BudgetExpensesDrawer` (despesas de uma rubrica: uma despesa **com fatura** abre o `InvoiceDetailDrawer` da lista de faturas, com reassociar via `BudgetItemPickerModal` e `IncidentDrawer`; só o gasto **sem fatura** abre o `BudgetExpenseDetailDrawer` — desde 2026-09-21), `BudgetExpenseFormDrawer`, 
| **Frontend** | `components/budget/` — `BudgetExpensesDrawer` (despesas de uma rubrica: uma despesa **com fatura** abre o `InvoiceDetailDrawer` da lista de faturas, com reassociar via `BudgetItemPickerModal` e `IncidentDrawer`; só o gasto **sem fatura** abre o `BudgetExpenseDetailDrawer` — desde 2026-09-21), `BudgetExpenseFormDrawer`, As credenciais com que a plataforma envia convites e recuperações de password. Antes da `V21` a
| **Frontend** | `components/budget/` — `BudgetExpensesDrawer` (despesas de uma rubrica: uma despesa **com fatura** abre o `InvoiceDetailDrawer` da lista de faturas, com reassociar via `BudgetItemPickerModal` e `IncidentDrawer`; só o gasto **sem fatura** abre o `BudgetExpenseDetailDrawer` — desde 2026-09-21), `BudgetExpenseFormDrawer`, tabela só era lida e a configuração entrava por `INSERT` à mão.
| **Frontend** | `components/budget/` — `BudgetExpensesDrawer` (despesas de uma rubrica: uma despesa **com fatura** abre o `InvoiceDetailDrawer` da lista de faturas, com reassociar via `BudgetItemPickerModal` e `IncidentDrawer`; só o gasto **sem fatura** abre o `BudgetExpenseDetailDrawer` — desde 2026-09-21), `BudgetExpenseFormDrawer`, 
| **Frontend** | `components/budget/` — `BudgetExpensesDrawer` (despesas de uma rubrica: uma despesa **com fatura** abre o `InvoiceDetailDrawer` da lista de faturas, com reassociar via `BudgetItemPickerModal` e `IncidentDrawer`; só o gasto **sem fatura** abre o `BudgetExpenseDetailDrawer` — desde 2026-09-21), `BudgetExpenseFormDrawer`, | Camada | Ficheiros |
| **Frontend** | `components/budget/` — `BudgetExpensesDrawer` (despesas de uma rubrica: uma despesa **com fatura** abre o `InvoiceDetailDrawer` da lista de faturas, com reassociar via `BudgetItemPickerModal` e `IncidentDrawer`; só o gasto **sem fatura** abre o `BudgetExpenseDetailDrawer` — desde 2026-09-21), `BudgetExpenseFormDrawer`, |---|---|
| **Frontend** | `components/budget/` — `BudgetExpensesDrawer` (despesas de uma rubrica: uma despesa **com fatura** abre o `InvoiceDetailDrawer` da lista de faturas, com reassociar via `BudgetItemPickerModal` e `IncidentDrawer`; só o gasto **sem fatura** abre o `BudgetExpenseDetailDrawer` — desde 2026-09-21), `BudgetExpenseFormDrawer`, | **Entrada** | menu do utilizador → grupo **Definições** → "Provedores de email" (`layouts/AppLayout.tsx`, só `ADMIN`) → `components/settings/EmailProvidersDrawer.tsx` |
| **Frontend** | `components/budget/` — `BudgetExpensesDrawer` (despesas de uma rubrica: uma despesa **com fatura** abre o `InvoiceDetailDrawer` da lista de faturas, com reassociar via `BudgetItemPickerModal` e `IncidentDrawer`; só o gasto **sem fatura** abre o `BudgetExpenseDetailDrawer` — desde 2026-09-21), `BudgetExpenseFormDrawer`, | **Frontend** | `services/emailProviderService.ts` · `types/emailProvider.ts` · `components/settings/emailProviderFormSchema.ts` |
| **Frontend** | `components/budget/` — `BudgetExpensesDrawer` (despesas de uma rubrica: uma despesa **com fatura** abre o `InvoiceDetailDrawer` da lista de faturas, com reassociar via `BudgetItemPickerModal` e `IncidentDrawer`; só o gasto **sem fatura** abre o `BudgetExpenseDetailDrawer` — desde 2026-09-21), `BudgetExpenseFormDrawer`, | **Backend** | `controller/EmailProviderController` · `service/email/EmailProviderService` · `service/email/EmailService` (envio) · `repository/email/EmailProviderRepository` · `model/email/EmailProvider` · `mapper/email/EmailProviderMapper` |
| **Frontend** | `components/budget/` — `BudgetExpensesDrawer` (despesas de uma rubrica: uma despesa **com fatura** abre o `InvoiceDetailDrawer` da lista de faturas, com reassociar via `BudgetItemPickerModal` e `IncidentDrawer`; só o gasto **sem fatura** abre o `BudgetExpenseDetailDrawer` — desde 2026-09-21), `BudgetExpenseFormDrawer`, | **Base de dados** | `settings.email_providers` — `V7`, `V21` (trigger de `updated_at`, índice de predefinido único, `entity_type`) |
| **Frontend** | `components/budget/` — `BudgetExpensesDrawer` (despesas de uma rubrica: uma despesa **com fatura** abre o `InvoiceDetailDrawer` da lista de faturas, com reassociar via `BudgetItemPickerModal` e `IncidentDrawer`; só o gasto **sem fatura** abre o `BudgetExpenseDetailDrawer` — desde 2026-09-21), `BudgetExpenseFormDrawer`, | **Detalhe** | [[api.md]] → "Provedores de email" · [[environment.md]] |
| **Frontend** | `components/budget/` — `BudgetExpensesDrawer` (despesas de uma rubrica: uma despesa **com fatura** abre o `InvoiceDetailDrawer` da lista de faturas, com reassociar via `BudgetItemPickerModal` e `IncidentDrawer`; só o gasto **sem fatura** abre o `BudgetExpenseDetailDrawer` — desde 2026-09-21), `BudgetExpenseFormDrawer`, 
| **Frontend** | `components/budget/` — `BudgetExpensesDrawer` (despesas de uma rubrica: uma despesa **com fatura** abre o `InvoiceDetailDrawer` da lista de faturas, com reassociar via `BudgetItemPickerModal` e `IncidentDrawer`; só o gasto **sem fatura** abre o `BudgetExpenseDetailDrawer` — desde 2026-09-21), `BudgetExpenseFormDrawer`, ## Orçamento de obra
| **Frontend** | `components/budget/` — `BudgetExpensesDrawer` (despesas de uma rubrica: uma despesa **com fatura** abre o `InvoiceDetailDrawer` da lista de faturas, com reassociar via `BudgetItemPickerModal` e `IncidentDrawer`; só o gasto **sem fatura** abre o `BudgetExpenseDetailDrawer` — desde 2026-09-21), `BudgetExpenseFormDrawer`, 
| **Frontend** | `components/budget/` — `BudgetExpensesDrawer` (despesas de uma rubrica: uma despesa **com fatura** abre o `InvoiceDetailDrawer` da lista de faturas, com reassociar via `BudgetItemPickerModal` e `IncidentDrawer`; só o gasto **sem fatura** abre o `BudgetExpenseDetailDrawer` — desde 2026-09-21), `BudgetExpenseFormDrawer`, A árvore de rubricas, importada do Excel do empreiteiro, e as despesas lançadas nela.
| **Frontend** | `components/budget/` — `BudgetExpensesDrawer` (despesas de uma rubrica: uma despesa **com fatura** abre o `InvoiceDetailDrawer` da lista de faturas, com reassociar via `BudgetItemPickerModal` e `IncidentDrawer`; só o gasto **sem fatura** abre o `BudgetExpenseDetailDrawer` — desde 2026-09-21), `BudgetExpenseFormDrawer`, 
| **Frontend** | `components/budget/` — `BudgetExpensesDrawer` (despesas de uma rubrica: uma despesa **com fatura** abre o `InvoiceDetailDrawer` da lista de faturas, com reassociar via `BudgetItemPickerModal` e `IncidentDrawer`; só o gasto **sem fatura** abre o `BudgetExpenseDetailDrawer` — desde 2026-09-21), `BudgetExpenseFormDrawer`, | Camada | Ficheiros |
| **Frontend** | `components/budget/` — `BudgetExpensesDrawer` (despesas de uma rubrica: uma despesa **com fatura** abre o `InvoiceDetailDrawer` da lista de faturas, com reassociar via `BudgetItemPickerModal` e `IncidentDrawer`; só o gasto **sem fatura** abre o `BudgetExpenseDetailDrawer` — desde 2026-09-21), `BudgetExpenseFormDrawer`, |---|---|
| **Frontend** | `components/budget/` — `BudgetExpensesDrawer` (despesas de uma rubrica: uma despesa **com fatura** abre o `InvoiceDetailDrawer` da lista de faturas, com reassociar via `BudgetItemPickerModal` e `IncidentDrawer`; só o gasto **sem fatura** abre o `BudgetExpenseDetailDrawer` — desde 2026-09-21), `BudgetExpenseFormDrawer`, | **Entrada** | rota `/backoffice/empreendimentos/:enterpriseId/budget` → `pages/backoffice/enterprise/ConstructionBudgetPage.tsx` |
| **Frontend** | `components/budget/` — `BudgetExpensesDrawer` (despesas de uma rubrica: uma despesa **com fatura** abre o `InvoiceDetailDrawer` da lista de faturas, com reassociar via `BudgetItemPickerModal` e `IncidentDrawer`; só o gasto **sem fatura** abre o `BudgetExpenseDetailDrawer` — desde 2026-09-21), `BudgetExpenseFormDrawer`, | **Frontend** | `components/budget/` — drawers de despesas/detalhe/formulário, `BudgetItemDrawer` (criar/editar rubrica, com datas), `BudgetMoveToModal`, `BudgetRecycleBinDrawer` (zona de recuperação), `BudgetImportModal`, `budgetTree.ts`, `budgetFormSchemas.ts` · `services/budgetService.ts` · `types/budget.ts` |
| **Frontend** | `components/budget/` — `BudgetExpensesDrawer` (despesas de uma rubrica: uma despesa **com fatura** abre o `InvoiceDetailDrawer` da lista de faturas, com reassociar via `BudgetItemPickerModal` e `IncidentDrawer`; só o gasto **sem fatura** abre o `BudgetExpenseDetailDrawer` — desde 2026-09-21), `BudgetExpenseFormDrawer`, | **Backend** | `enterprises/controller/ConstructionBudgetItemController` + `ConstructionExpenseController` · `service/ConstructionBudgetItemService` · `ConstructionExpenseService` · `BudgetExcelImportService` / `BudgetExcelExportService` / `DespesasExcelImportService` (Apache POI) · `InvoiceDocumentsExportService` (a `Faturas/Lançadas/` do zip, nomes §7) |
| **Frontend** | `components/budget/` — `BudgetExpensesDrawer` (despesas de uma rubrica: uma despesa **com fatura** abre o `InvoiceDetailDrawer` da lista de faturas, com reassociar via `BudgetItemPickerModal` e `IncidentDrawer`; só o gasto **sem fatura** abre o `BudgetExpenseDetailDrawer` — desde 2026-09-21), `BudgetExpenseFormDrawer`, | **Base de dados** | `worksite.construction_budget_item` (auto-referenciada) + `construction_expense` — `V15`; soft delete (`deleted_at`) — `V36` |
| **Frontend** | `components/budget/` — `BudgetExpensesDrawer` (despesas de uma rubrica: uma despesa **com fatura** abre o `InvoiceDetailDrawer` da lista de faturas, com reassociar via `BudgetItemPickerModal` e `IncidentDrawer`; só o gasto **sem fatura** abre o `BudgetExpenseDetailDrawer` — desde 2026-09-21), `BudgetExpenseFormDrawer`, | **Detalhe** | [[api.md]] → "Orçamento de Construção" |
| **Frontend** | `components/budget/` — `BudgetExpensesDrawer` (despesas de uma rubrica: uma despesa **com fatura** abre o `InvoiceDetailDrawer` da lista de faturas, com reassociar via `BudgetItemPickerModal` e `IncidentDrawer`; só o gasto **sem fatura** abre o `BudgetExpenseDetailDrawer` — desde 2026-09-21), `BudgetExpenseFormDrawer`, 
| **Frontend** | `components/budget/` — `BudgetExpensesDrawer` (despesas de uma rubrica: uma despesa **com fatura** abre o `InvoiceDetailDrawer` da lista de faturas, com reassociar via `BudgetItemPickerModal` e `IncidentDrawer`; só o gasto **sem fatura** abre o `BudgetExpenseDetailDrawer` — desde 2026-09-21), `BudgetExpenseFormDrawer`, ## Projetos (Enterprises)
| **Frontend** | `components/budget/` — `BudgetExpensesDrawer` (despesas de uma rubrica: uma despesa **com fatura** abre o `InvoiceDetailDrawer` da lista de faturas, com reassociar via `BudgetItemPickerModal` e `IncidentDrawer`; só o gasto **sem fatura** abre o `BudgetExpenseDetailDrawer` — desde 2026-09-21), `BudgetExpenseFormDrawer`, 
| **Frontend** | `components/budget/` — `BudgetExpensesDrawer` (despesas de uma rubrica: uma despesa **com fatura** abre o `InvoiceDetailDrawer` da lista de faturas, com reassociar via `BudgetItemPickerModal` e `IncidentDrawer`; só o gasto **sem fatura** abre o `BudgetExpenseDetailDrawer` — desde 2026-09-21), `BudgetExpenseFormDrawer`, A obra. O nome `enterprises` ficou do Property-Management — ver [[architecture.md]].
| **Frontend** | `components/budget/` — `BudgetExpensesDrawer` (despesas de uma rubrica: uma despesa **com fatura** abre o `InvoiceDetailDrawer` da lista de faturas, com reassociar via `BudgetItemPickerModal` e `IncidentDrawer`; só o gasto **sem fatura** abre o `BudgetExpenseDetailDrawer` — desde 2026-09-21), `BudgetExpenseFormDrawer`, 
| **Frontend** | `components/budget/` — `BudgetExpensesDrawer` (despesas de uma rubrica: uma despesa **com fatura** abre o `InvoiceDetailDrawer` da lista de faturas, com reassociar via `BudgetItemPickerModal` e `IncidentDrawer`; só o gasto **sem fatura** abre o `BudgetExpenseDetailDrawer` — desde 2026-09-21), `BudgetExpenseFormDrawer`, | Camada | Ficheiros |
| **Frontend** | `components/budget/` — `BudgetExpensesDrawer` (despesas de uma rubrica: uma despesa **com fatura** abre o `InvoiceDetailDrawer` da lista de faturas, com reassociar via `BudgetItemPickerModal` e `IncidentDrawer`; só o gasto **sem fatura** abre o `BudgetExpenseDetailDrawer` — desde 2026-09-21), `BudgetExpenseFormDrawer`, |---|---|
| **Frontend** | `components/budget/` — `BudgetExpensesDrawer` (despesas de uma rubrica: uma despesa **com fatura** abre o `InvoiceDetailDrawer` da lista de faturas, com reassociar via `BudgetItemPickerModal` e `IncidentDrawer`; só o gasto **sem fatura** abre o `BudgetExpenseDetailDrawer` — desde 2026-09-21), `BudgetExpenseFormDrawer`, | **Entrada** | rota `/backoffice/empreendimentos` → `pages/enterprises/EnterprisesList.tsx` — ações por linha: "Ver detalhes" (drawer), "Ver orçamento" (→ `/backoffice/empreendimentos/{id}/budget`, desde 2026-09-21), "Eliminar" |
| **Frontend** | `components/budget/` — `BudgetExpensesDrawer` (despesas de uma rubrica: uma despesa **com fatura** abre o `InvoiceDetailDrawer` da lista de faturas, com reassociar via `BudgetItemPickerModal` e `IncidentDrawer`; só o gasto **sem fatura** abre o `BudgetExpenseDetailDrawer` — desde 2026-09-21), `BudgetExpenseFormDrawer`, | **Frontend** | `components/enterprise/` — `create/` (secções + `enterpriseFormSchema.ts`), `edit/` (cards), `CreateEnterpriseDrawer`, `EnterpriseViewDrawer` · `services/enterpriseService.ts` |
| **Frontend** | `components/budget/` — `BudgetExpensesDrawer` (despesas de uma rubrica: uma despesa **com fatura** abre o `InvoiceDetailDrawer` da lista de faturas, com reassociar via `BudgetItemPickerModal` e `IncidentDrawer`; só o gasto **sem fatura** abre o `BudgetExpenseDetailDrawer` — desde 2026-09-21), `BudgetExpenseFormDrawer`, | **Backend** | `enterprises/controller/EnterpriseController` + `EntrepriseRelationsController` · `service/EnterpriseService` · `mapper/EnterpriseMapper` |
| **Frontend** | `components/budget/` — `BudgetExpensesDrawer` (despesas de uma rubrica: uma despesa **com fatura** abre o `InvoiceDetailDrawer` da lista de faturas, com reassociar via `BudgetItemPickerModal` e `IncidentDrawer`; só o gasto **sem fatura** abre o `BudgetExpenseDetailDrawer` — desde 2026-09-21), `BudgetExpenseFormDrawer`, | **Base de dados** | `worksite.enterprises` (+ `enterprises_location`, `enterprises_media`) — `V4`, `V12` |
| **Frontend** | `components/budget/` — `BudgetExpensesDrawer` (despesas de uma rubrica: uma despesa **com fatura** abre o `InvoiceDetailDrawer` da lista de faturas, com reassociar via `BudgetItemPickerModal` e `IncidentDrawer`; só o gasto **sem fatura** abre o `BudgetExpenseDetailDrawer` — desde 2026-09-21), `BudgetExpenseFormDrawer`, 
| **Frontend** | `components/budget/` — `BudgetExpensesDrawer` (despesas de uma rubrica: uma despesa **com fatura** abre o `InvoiceDetailDrawer` da lista de faturas, com reassociar via `BudgetItemPickerModal` e `IncidentDrawer`; só o gasto **sem fatura** abre o `BudgetExpenseDetailDrawer` — desde 2026-09-21), `BudgetExpenseFormDrawer`, ## Tarefas
| **Frontend** | `components/budget/` — `BudgetExpensesDrawer` (despesas de uma rubrica: uma despesa **com fatura** abre o `InvoiceDetailDrawer` da lista de faturas, com reassociar via `BudgetItemPickerModal` e `IncidentDrawer`; só o gasto **sem fatura** abre o `BudgetExpenseDetailDrawer` — desde 2026-09-21), `BudgetExpenseFormDrawer`, 
| **Frontend** | `components/budget/` — `BudgetExpensesDrawer` (despesas de uma rubrica: uma despesa **com fatura** abre o `InvoiceDetailDrawer` da lista de faturas, com reassociar via `BudgetItemPickerModal` e `IncidentDrawer`; só o gasto **sem fatura** abre o `BudgetExpenseDetailDrawer` — desde 2026-09-21), `BudgetExpenseFormDrawer`, Tarefas standalone, isoladas no seu próprio schema — sem ligação a obra nenhuma.
| **Frontend** | `components/budget/` — `BudgetExpensesDrawer` (despesas de uma rubrica: uma despesa **com fatura** abre o `InvoiceDetailDrawer` da lista de faturas, com reassociar via `BudgetItemPickerModal` e `IncidentDrawer`; só o gasto **sem fatura** abre o `BudgetExpenseDetailDrawer` — desde 2026-09-21), `BudgetExpenseFormDrawer`, 
| **Frontend** | `components/budget/` — `BudgetExpensesDrawer` (despesas de uma rubrica: uma despesa **com fatura** abre o `InvoiceDetailDrawer` da lista de faturas, com reassociar via `BudgetItemPickerModal` e `IncidentDrawer`; só o gasto **sem fatura** abre o `BudgetExpenseDetailDrawer` — desde 2026-09-21), `BudgetExpenseFormDrawer`, | Camada | Ficheiros |
| **Frontend** | `components/budget/` — `BudgetExpensesDrawer` (despesas de uma rubrica: uma despesa **com fatura** abre o `InvoiceDetailDrawer` da lista de faturas, com reassociar via `BudgetItemPickerModal` e `IncidentDrawer`; só o gasto **sem fatura** abre o `BudgetExpenseDetailDrawer` — desde 2026-09-21), `BudgetExpenseFormDrawer`, |---|---|
| **Frontend** | `components/budget/` — `BudgetExpensesDrawer` (despesas de uma rubrica: uma despesa **com fatura** abre o `InvoiceDetailDrawer` da lista de faturas, com reassociar via `BudgetItemPickerModal` e `IncidentDrawer`; só o gasto **sem fatura** abre o `BudgetExpenseDetailDrawer` — desde 2026-09-21), `BudgetExpenseFormDrawer`, | **Entrada** | rota `/backoffice/tasks` → `pages/backoffice/TasksPage.tsx` |
| **Frontend** | `components/budget/` — `BudgetExpensesDrawer` (despesas de uma rubrica: uma despesa **com fatura** abre o `InvoiceDetailDrawer` da lista de faturas, com reassociar via `BudgetItemPickerModal` e `IncidentDrawer`; só o gasto **sem fatura** abre o `BudgetExpenseDetailDrawer` — desde 2026-09-21), `BudgetExpenseFormDrawer`, | **Frontend** | `components/tasks/` — `TasksList`, `TaskFormDrawer`, `TaskDetailDrawer` · `services/taskService.ts` |
| **Frontend** | `components/budget/` — `BudgetExpensesDrawer` (despesas de uma rubrica: uma despesa **com fatura** abre o `InvoiceDetailDrawer` da lista de faturas, com reassociar via `BudgetItemPickerModal` e `IncidentDrawer`; só o gasto **sem fatura** abre o `BudgetExpenseDetailDrawer` — desde 2026-09-21), `BudgetExpenseFormDrawer`, | **Backend** | `controller/TaskController` · `service/TaskService` · `model/Task` + `TaskAssignee` |
| **Frontend** | `components/budget/` — `BudgetExpensesDrawer` (despesas de uma rubrica: uma despesa **com fatura** abre o `InvoiceDetailDrawer` da lista de faturas, com reassociar via `BudgetItemPickerModal` e `IncidentDrawer`; só o gasto **sem fatura** abre o `BudgetExpenseDetailDrawer` — desde 2026-09-21), `BudgetExpenseFormDrawer`, | **Base de dados** | schema `tasks` — `V14` |
| **Frontend** | `components/budget/` — `BudgetExpensesDrawer` (despesas de uma rubrica: uma despesa **com fatura** abre o `InvoiceDetailDrawer` da lista de faturas, com reassociar via `BudgetItemPickerModal` e `IncidentDrawer`; só o gasto **sem fatura** abre o `BudgetExpenseDetailDrawer` — desde 2026-09-21), `BudgetExpenseFormDrawer`, 
| **Frontend** | `components/budget/` — `BudgetExpensesDrawer` (despesas de uma rubrica: uma despesa **com fatura** abre o `InvoiceDetailDrawer` da lista de faturas, com reassociar via `BudgetItemPickerModal` e `IncidentDrawer`; só o gasto **sem fatura** abre o `BudgetExpenseDetailDrawer` — desde 2026-09-21), `BudgetExpenseFormDrawer`, ## Equipa, perfis e convites
| **Frontend** | `components/budget/` — `BudgetExpensesDrawer` (despesas de uma rubrica: uma despesa **com fatura** abre o `InvoiceDetailDrawer` da lista de faturas, com reassociar via `BudgetItemPickerModal` e `IncidentDrawer`; só o gasto **sem fatura** abre o `BudgetExpenseDetailDrawer` — desde 2026-09-21), `BudgetExpenseFormDrawer`, 
| **Frontend** | `components/budget/` — `BudgetExpensesDrawer` (despesas de uma rubrica: uma despesa **com fatura** abre o `InvoiceDetailDrawer` da lista de faturas, com reassociar via `BudgetItemPickerModal` e `IncidentDrawer`; só o gasto **sem fatura** abre o `BudgetExpenseDetailDrawer` — desde 2026-09-21), `BudgetExpenseFormDrawer`, | Camada | Ficheiros |
| **Frontend** | `components/budget/` — `BudgetExpensesDrawer` (despesas de uma rubrica: uma despesa **com fatura** abre o `InvoiceDetailDrawer` da lista de faturas, com reassociar via `BudgetItemPickerModal` e `IncidentDrawer`; só o gasto **sem fatura** abre o `BudgetExpenseDetailDrawer` — desde 2026-09-21), `BudgetExpenseFormDrawer`, |---|---|
| **Frontend** | `components/budget/` — `BudgetExpensesDrawer` (despesas de uma rubrica: uma despesa **com fatura** abre o `InvoiceDetailDrawer` da lista de faturas, com reassociar via `BudgetItemPickerModal` e `IncidentDrawer`; só o gasto **sem fatura** abre o `BudgetExpenseDetailDrawer` — desde 2026-09-21), `BudgetExpenseFormDrawer`, | **Entrada** | rotas `/backoffice/funcionarios` e `/funcionarios/:id` · convite público em `/accept-invite` |
| **Frontend** | `components/budget/` — `BudgetExpensesDrawer` (despesas de uma rubrica: uma despesa **com fatura** abre o `InvoiceDetailDrawer` da lista de faturas, com reassociar via `BudgetItemPickerModal` e `IncidentDrawer`; só o gasto **sem fatura** abre o `BudgetExpenseDetailDrawer` — desde 2026-09-21), `BudgetExpenseFormDrawer`, | **Frontend** | `pages/backoffice/EmployeesList.tsx` · `employee/EmployeeProfilePage.tsx` · `pages/AcceptInvitePage.tsx` · `components/employees/`, `components/profile/`, `components/invites/InvitesDrawer` · `services/profileService.ts` + `adminService.ts` |
| **Frontend** | `components/budget/` — `BudgetExpensesDrawer` (despesas de uma rubrica: uma despesa **com fatura** abre o `InvoiceDetailDrawer` da lista de faturas, com reassociar via `BudgetItemPickerModal` e `IncidentDrawer`; só o gasto **sem fatura** abre o `BudgetExpenseDetailDrawer` — desde 2026-09-21), `BudgetExpenseFormDrawer`, | **Backend** | `controller/EmployeesController` · `ProfileController` · `AdminAuthController` (envio de convites) · `AuthController` + `service/InviteService` (aceitação) · `service/employee/` · `ProfileService` · `service/email/` |
| **Frontend** | `components/budget/` — `BudgetExpensesDrawer` (despesas de uma rubrica: uma despesa **com fatura** abre o `InvoiceDetailDrawer` da lista de faturas, com reassociar via `BudgetItemPickerModal` e `IncidentDrawer`; só o gasto **sem fatura** abre o `BudgetExpenseDetailDrawer` — desde 2026-09-21), `BudgetExpenseFormDrawer`, | **Base de dados** | `worksite.profile` (`V3`) · `settings.pending_invites` + `email_providers` (`V7`) |
| **Frontend** | `components/budget/` — `BudgetExpensesDrawer` (despesas de uma rubrica: uma despesa **com fatura** abre o `InvoiceDetailDrawer` da lista de faturas, com reassociar via `BudgetItemPickerModal` e `IncidentDrawer`; só o gasto **sem fatura** abre o `BudgetExpenseDetailDrawer` — desde 2026-09-21), `BudgetExpenseFormDrawer`, 
| **Frontend** | `components/budget/` — `BudgetExpensesDrawer` (despesas de uma rubrica: uma despesa **com fatura** abre o `InvoiceDetailDrawer` da lista de faturas, com reassociar via `BudgetItemPickerModal` e `IncidentDrawer`; só o gasto **sem fatura** abre o `BudgetExpenseDetailDrawer` — desde 2026-09-21), `BudgetExpenseFormDrawer`, ## Notificações
| **Frontend** | `components/budget/` — `BudgetExpensesDrawer` (despesas de uma rubrica: uma despesa **com fatura** abre o `InvoiceDetailDrawer` da lista de faturas, com reassociar via `BudgetItemPickerModal` e `IncidentDrawer`; só o gasto **sem fatura** abre o `BudgetExpenseDetailDrawer` — desde 2026-09-21), `BudgetExpenseFormDrawer`, 
| **Frontend** | `components/budget/` — `BudgetExpensesDrawer` (despesas de uma rubrica: uma despesa **com fatura** abre o `InvoiceDetailDrawer` da lista de faturas, com reassociar via `BudgetItemPickerModal` e `IncidentDrawer`; só o gasto **sem fatura** abre o `BudgetExpenseDetailDrawer` — desde 2026-09-21), `BudgetExpenseFormDrawer`, | Camada | Ficheiros |
| **Frontend** | `components/budget/` — `BudgetExpensesDrawer` (despesas de uma rubrica: uma despesa **com fatura** abre o `InvoiceDetailDrawer` da lista de faturas, com reassociar via `BudgetItemPickerModal` e `IncidentDrawer`; só o gasto **sem fatura** abre o `BudgetExpenseDetailDrawer` — desde 2026-09-21), `BudgetExpenseFormDrawer`, |---|---|
| **Frontend** | `components/budget/` — `BudgetExpensesDrawer` (despesas de uma rubrica: uma despesa **com fatura** abre o `InvoiceDetailDrawer` da lista de faturas, com reassociar via `BudgetItemPickerModal` e `IncidentDrawer`; só o gasto **sem fatura** abre o `BudgetExpenseDetailDrawer` — desde 2026-09-21), `BudgetExpenseFormDrawer`, | **Entrada** | o sino do cabeçalho — `components/notifications/NotificationBell.tsx` (`iconFor` escolhe o ícone por `type`; sem polling) |
| **Frontend** | `components/budget/` — `BudgetExpensesDrawer` (despesas de uma rubrica: uma despesa **com fatura** abre o `InvoiceDetailDrawer` da lista de faturas, com reassociar via `BudgetItemPickerModal` e `IncidentDrawer`; só o gasto **sem fatura** abre o `BudgetExpenseDetailDrawer` — desde 2026-09-21), `BudgetExpenseFormDrawer`, | **Frontend** | `services/notificationInboxService.ts` · `types/notification.ts` |
| **Frontend** | `components/budget/` — `BudgetExpensesDrawer` (despesas de uma rubrica: uma despesa **com fatura** abre o `InvoiceDetailDrawer` da lista de faturas, com reassociar via `BudgetItemPickerModal` e `IncidentDrawer`; só o gasto **sem fatura** abre o `BudgetExpenseDetailDrawer` — desde 2026-09-21), `BudgetExpenseFormDrawer`, | **Backend** | `notifications/controller/NotificationController` · `service/NotificationService` (escrita chamada pelos serviços de domínio, na mesma transação) · `service/BudgetItemDeadlineNotifier` (o gatilho por job) · `repository/NotificationRepository` · `model/Notification` (constantes `TYPE_*`) |
| **Frontend** | `components/budget/` — `BudgetExpensesDrawer` (despesas de uma rubrica: uma despesa **com fatura** abre o `InvoiceDetailDrawer` da lista de faturas, com reassociar via `BudgetItemPickerModal` e `IncidentDrawer`; só o gasto **sem fatura** abre o `BudgetExpenseDetailDrawer` — desde 2026-09-21), `BudgetExpenseFormDrawer`, | **Jobs agendados** | `enterprises/config/BudgetItemDeadlineNotifierConfig` (07:00 + arranque) — os outros dois jobs do projeto são `security/RevokedTokenCleanupConfig` (limpeza de tokens) e `enterprises/config/ConstructionBudgetItemPurgeConfig` (purga às 3h) |
| **Frontend** | `components/budget/` — `BudgetExpensesDrawer` (despesas de uma rubrica: uma despesa **com fatura** abre o `InvoiceDetailDrawer` da lista de faturas, com reassociar via `BudgetItemPickerModal` e `IncidentDrawer`; só o gasto **sem fatura** abre o `BudgetExpenseDetailDrawer` — desde 2026-09-21), `BudgetExpenseFormDrawer`, | **Base de dados** | `worksite.notification` — `V20` |
| **Frontend** | `components/budget/` — `BudgetExpensesDrawer` (despesas de uma rubrica: uma despesa **com fatura** abre o `InvoiceDetailDrawer` da lista de faturas, com reassociar via `BudgetItemPickerModal` e `IncidentDrawer`; só o gasto **sem fatura** abre o `BudgetExpenseDetailDrawer` — desde 2026-09-21), `BudgetExpenseFormDrawer`, | **Detalhe** | [[api.md]] → "Quem gera notificações" |
| **Frontend** | `components/budget/` — `BudgetExpensesDrawer` (despesas de uma rubrica: uma despesa **com fatura** abre o `InvoiceDetailDrawer` da lista de faturas, com reassociar via `BudgetItemPickerModal` e `IncidentDrawer`; só o gasto **sem fatura** abre o `BudgetExpenseDetailDrawer` — desde 2026-09-21), `BudgetExpenseFormDrawer`, 
| **Frontend** | `components/budget/` — `BudgetExpensesDrawer` (despesas de uma rubrica: uma despesa **com fatura** abre o `InvoiceDetailDrawer` da lista de faturas, com reassociar via `BudgetItemPickerModal` e `IncidentDrawer`; só o gasto **sem fatura** abre o `BudgetExpenseDetailDrawer` — desde 2026-09-21), `BudgetExpenseFormDrawer`, ## Autenticação
| **Frontend** | `components/budget/` — `BudgetExpensesDrawer` (despesas de uma rubrica: uma despesa **com fatura** abre o `InvoiceDetailDrawer` da lista de faturas, com reassociar via `BudgetItemPickerModal` e `IncidentDrawer`; só o gasto **sem fatura** abre o `BudgetExpenseDetailDrawer` — desde 2026-09-21), `BudgetExpenseFormDrawer`, 
| **Frontend** | `components/budget/` — `BudgetExpensesDrawer` (despesas de uma rubrica: uma despesa **com fatura** abre o `InvoiceDetailDrawer` da lista de faturas, com reassociar via `BudgetItemPickerModal` e `IncidentDrawer`; só o gasto **sem fatura** abre o `BudgetExpenseDetailDrawer` — desde 2026-09-21), `BudgetExpenseFormDrawer`, JWT do Supabase validado localmente, cookies HttpOnly. **Sem SDK do Supabase no frontend.**
| **Frontend** | `components/budget/` — `BudgetExpensesDrawer` (despesas de uma rubrica: uma despesa **com fatura** abre o `InvoiceDetailDrawer` da lista de faturas, com reassociar via `BudgetItemPickerModal` e `IncidentDrawer`; só o gasto **sem fatura** abre o `BudgetExpenseDetailDrawer` — desde 2026-09-21), `BudgetExpenseFormDrawer`, 
| **Frontend** | `components/budget/` — `BudgetExpensesDrawer` (despesas de uma rubrica: uma despesa **com fatura** abre o `InvoiceDetailDrawer` da lista de faturas, com reassociar via `BudgetItemPickerModal` e `IncidentDrawer`; só o gasto **sem fatura** abre o `BudgetExpenseDetailDrawer` — desde 2026-09-21), `BudgetExpenseFormDrawer`, | Camada | Ficheiros |
| **Frontend** | `components/budget/` — `BudgetExpensesDrawer` (despesas de uma rubrica: uma despesa **com fatura** abre o `InvoiceDetailDrawer` da lista de faturas, com reassociar via `BudgetItemPickerModal` e `IncidentDrawer`; só o gasto **sem fatura** abre o `BudgetExpenseDetailDrawer` — desde 2026-09-21), `BudgetExpenseFormDrawer`, |---|---|
| **Frontend** | `components/budget/` — `BudgetExpensesDrawer` (despesas de uma rubrica: uma despesa **com fatura** abre o `InvoiceDetailDrawer` da lista de faturas, com reassociar via `BudgetItemPickerModal` e `IncidentDrawer`; só o gasto **sem fatura** abre o `BudgetExpenseDetailDrawer` — desde 2026-09-21), `BudgetExpenseFormDrawer`, | **Entrada** | `/login` → `pages/Login.tsx` → `/loading` → `PrivateRoute.tsx` · recuperação em `/forgot-password` → `/reset-password` (`pages/ForgotPassword.tsx`, `pages/ResetPassword.tsx`) |
| **Frontend** | `components/budget/` — `BudgetExpensesDrawer` (despesas de uma rubrica: uma despesa **com fatura** abre o `InvoiceDetailDrawer` da lista de faturas, com reassociar via `BudgetItemPickerModal` e `IncidentDrawer`; só o gasto **sem fatura** abre o `BudgetExpenseDetailDrawer` — desde 2026-09-21), `BudgetExpenseFormDrawer`, | **Frontend** | `context/AuthContext.tsx` · `hooks/useAuth.ts` · `services/authService.ts` (login, `requestPasswordReset`, `resetPassword`) · `api.ts` (refresh automático em 401) |
| **Frontend** | `components/budget/` — `BudgetExpensesDrawer` (despesas de uma rubrica: uma despesa **com fatura** abre o `InvoiceDetailDrawer` da lista de faturas, com reassociar via `BudgetItemPickerModal` e `IncidentDrawer`; só o gasto **sem fatura** abre o `BudgetExpenseDetailDrawer` — desde 2026-09-21), `BudgetExpenseFormDrawer`, | **Backend** | `controller/AuthController` · `service/SupabaseAuthService` · `service/PasswordResetService` · `security/` (`SecurityConfig`, `AuthContext`, `AccountLockFilter`, `TokenRevocationFilter`) |
| **Frontend** | `components/budget/` — `BudgetExpensesDrawer` (despesas de uma rubrica: uma despesa **com fatura** abre o `InvoiceDetailDrawer` da lista de faturas, com reassociar via `BudgetItemPickerModal` e `IncidentDrawer`; só o gasto **sem fatura** abre o `BudgetExpenseDetailDrawer` — desde 2026-09-21), `BudgetExpenseFormDrawer`, | **Base de dados** | `worksite.revoked_token` (`V6`), `profile.role`, `profile.last_token_reset_at`, `settings.password_reset_tokens` (`V22`) |
| **Frontend** | `components/budget/` — `BudgetExpensesDrawer` (despesas de uma rubrica: uma despesa **com fatura** abre o `InvoiceDetailDrawer` da lista de faturas, com reassociar via `BudgetItemPickerModal` e `IncidentDrawer`; só o gasto **sem fatura** abre o `BudgetExpenseDetailDrawer` — desde 2026-09-21), `BudgetExpenseFormDrawer`, | **Detalhe** | [[security.md]] |
| **Frontend** | `components/budget/` — `BudgetExpensesDrawer` (despesas de uma rubrica: uma despesa **com fatura** abre o `InvoiceDetailDrawer` da lista de faturas, com reassociar via `BudgetItemPickerModal` e `IncidentDrawer`; só o gasto **sem fatura** abre o `BudgetExpenseDetailDrawer` — desde 2026-09-21), `BudgetExpenseFormDrawer`, 
| **Frontend** | `components/budget/` — `BudgetExpensesDrawer` (despesas de uma rubrica: uma despesa **com fatura** abre o `InvoiceDetailDrawer` da lista de faturas, com reassociar via `BudgetItemPickerModal` e `IncidentDrawer`; só o gasto **sem fatura** abre o `BudgetExpenseDetailDrawer` — desde 2026-09-21), `BudgetExpenseFormDrawer`, ---
| **Frontend** | `components/budget/` — `BudgetExpensesDrawer` (despesas de uma rubrica: uma despesa **com fatura** abre o `InvoiceDetailDrawer` da lista de faturas, com reassociar via `BudgetItemPickerModal` e `IncidentDrawer`; só o gasto **sem fatura** abre o `BudgetExpenseDetailDrawer` — desde 2026-09-21), `BudgetExpenseFormDrawer`, 
| **Frontend** | `components/budget/` — `BudgetExpensesDrawer` (despesas de uma rubrica: uma despesa **com fatura** abre o `InvoiceDetailDrawer` da lista de faturas, com reassociar via `BudgetItemPickerModal` e `IncidentDrawer`; só o gasto **sem fatura** abre o `BudgetExpenseDetailDrawer` — desde 2026-09-21), `BudgetExpenseFormDrawer`, ## Onde procurar, por sintoma
| **Frontend** | `components/budget/` — `BudgetExpensesDrawer` (despesas de uma rubrica: uma despesa **com fatura** abre o `InvoiceDetailDrawer` da lista de faturas, com reassociar via `BudgetItemPickerModal` e `IncidentDrawer`; só o gasto **sem fatura** abre o `BudgetExpenseDetailDrawer` — desde 2026-09-21), `BudgetExpenseFormDrawer`, 
| **Frontend** | `components/budget/` — `BudgetExpensesDrawer` (despesas de uma rubrica: uma despesa **com fatura** abre o `InvoiceDetailDrawer` da lista de faturas, com reassociar via `BudgetItemPickerModal` e `IncidentDrawer`; só o gasto **sem fatura** abre o `BudgetExpenseDetailDrawer` — desde 2026-09-21), `BudgetExpenseFormDrawer`, A tabela que poupa mais tempo: o problema como se descreve em voz alta → o ficheiro onde
| **Frontend** | `components/budget/` — `BudgetExpensesDrawer` (despesas de uma rubrica: uma despesa **com fatura** abre o `InvoiceDetailDrawer` da lista de faturas, com reassociar via `BudgetItemPickerModal` e `IncidentDrawer`; só o gasto **sem fatura** abre o `BudgetExpenseDetailDrawer` — desde 2026-09-21), `BudgetExpenseFormDrawer`, começar a olhar.
| **Frontend** | `components/budget/` — `BudgetExpensesDrawer` (despesas de uma rubrica: uma despesa **com fatura** abre o `InvoiceDetailDrawer` da lista de faturas, com reassociar via `BudgetItemPickerModal` e `IncidentDrawer`; só o gasto **sem fatura** abre o `BudgetExpenseDetailDrawer` — desde 2026-09-21), `BudgetExpenseFormDrawer`, 
| **Frontend** | `components/budget/` — `BudgetExpensesDrawer` (despesas de uma rubrica: uma despesa **com fatura** abre o `InvoiceDetailDrawer` da lista de faturas, com reassociar via `BudgetItemPickerModal` e `IncidentDrawer`; só o gasto **sem fatura** abre o `BudgetExpenseDetailDrawer` — desde 2026-09-21), `BudgetExpenseFormDrawer`, | "O que se passa é que…" | Começa em |
| **Frontend** | `components/budget/` — `BudgetExpensesDrawer` (despesas de uma rubrica: uma despesa **com fatura** abre o `InvoiceDetailDrawer` da lista de faturas, com reassociar via `BudgetItemPickerModal` e `IncidentDrawer`; só o gasto **sem fatura** abre o `BudgetExpenseDetailDrawer` — desde 2026-09-21), `BudgetExpenseFormDrawer`, |---|---|
| **Frontend** | `components/budget/` — `BudgetExpensesDrawer` (despesas de uma rubrica: uma despesa **com fatura** abre o `InvoiceDetailDrawer` da lista de faturas, com reassociar via `BudgetItemPickerModal` e `IncidentDrawer`; só o gasto **sem fatura** abre o `BudgetExpenseDetailDrawer` — desde 2026-09-21), `BudgetExpenseFormDrawer`, | o QR de uma fatura não é lido | `AtInvoiceQrService` (escalada de 4 degraus) → `WeChatQrCodeService` |
| **Frontend** | `components/budget/` — `BudgetExpensesDrawer` (despesas de uma rubrica: uma despesa **com fatura** abre o `InvoiceDetailDrawer` da lista de faturas, com reassociar via `BudgetItemPickerModal` e `IncidentDrawer`; só o gasto **sem fatura** abre o `BudgetExpenseDetailDrawer` — desde 2026-09-21), `BudgetExpenseFormDrawer`, | a fatura entrou mas sem fornecedor | não é bug: o QR não traz o nome — `SupplierService` / `SuppliersDrawer` |
| **Frontend** | `components/budget/` — `BudgetExpensesDrawer` (despesas de uma rubrica: uma despesa **com fatura** abre o `InvoiceDetailDrawer` da lista de faturas, com reassociar via `BudgetItemPickerModal` e `IncidentDrawer`; só o gasto **sem fatura** abre o `BudgetExpenseDetailDrawer` — desde 2026-09-21), `BudgetExpenseFormDrawer`, | a fatura entrou "por rever" | `needsReview` é derivado (falta data **ou** total) — `ConstructionInvoice.needsReview()` |
| **Frontend** | `components/budget/` — `BudgetExpensesDrawer` (despesas de uma rubrica: uma despesa **com fatura** abre o `InvoiceDetailDrawer` da lista de faturas, com reassociar via `BudgetItemPickerModal` e `IncidentDrawer`; só o gasto **sem fatura** abre o `BudgetExpenseDetailDrawer` — desde 2026-09-21), `BudgetExpenseFormDrawer`, | diz que a fatura é duplicada e não devia | `ConstructionInvoiceService#rejectIfDuplicate` — três chaves, ver [[api.md]] → "Duplicados" |
| **Frontend** | `components/budget/` — `BudgetExpensesDrawer` (despesas de uma rubrica: uma despesa **com fatura** abre o `InvoiceDetailDrawer` da lista de faturas, com reassociar via `BudgetItemPickerModal` e `IncidentDrawer`; só o gasto **sem fatura** abre o `BudgetExpenseDetailDrawer` — desde 2026-09-21), `BudgetExpenseFormDrawer`, | o erro que aparece no ecrã não diz nada | `errors/errorMessages.ts` (espelha `dto/error/ErrorCode.java` 1:1) |
| **Frontend** | `components/budget/` — `BudgetExpensesDrawer` (despesas de uma rubrica: uma despesa **com fatura** abre o `InvoiceDetailDrawer` da lista de faturas, com reassociar via `BudgetItemPickerModal` e `IncidentDrawer`; só o gasto **sem fatura** abre o `BudgetExpenseDetailDrawer` — desde 2026-09-21), `BudgetExpenseFormDrawer`, | o pedido devolve 401/403 | `security/SecurityConfig` + o `@PreAuthorize` do controller |
| **Frontend** | `components/budget/` — `BudgetExpensesDrawer` (despesas de uma rubrica: uma despesa **com fatura** abre o `InvoiceDetailDrawer` da lista de faturas, com reassociar via `BudgetItemPickerModal` e `IncidentDrawer`; só o gasto **sem fatura** abre o `BudgetExpenseDetailDrawer` — desde 2026-09-21), `BudgetExpenseFormDrawer`, | o ficheiro não abre / a imagem não aparece | signed URLs — `integrations/supabase/SignedUrlService`, `components/image/AuthenticatedImage` |
| **Frontend** | `components/budget/` — `BudgetExpensesDrawer` (despesas de uma rubrica: uma despesa **com fatura** abre o `InvoiceDetailDrawer` da lista de faturas, com reassociar via `BudgetItemPickerModal` e `IncidentDrawer`; só o gasto **sem fatura** abre o `BudgetExpenseDetailDrawer` — desde 2026-09-21), `BudgetExpenseFormDrawer`, | a cor/espaçamento está fora do sistema | tokens `--ind-*` em `index.css` (espelhados em `theme.ts`) — ver [[skills/references/design/backoffice-tokens-and-colors]] |
| **Frontend** | `components/budget/` — `BudgetExpensesDrawer` (despesas de uma rubrica: uma despesa **com fatura** abre o `InvoiceDetailDrawer` da lista de faturas, com reassociar via `BudgetItemPickerModal` e `IncidentDrawer`; só o gasto **sem fatura** abre o `BudgetExpenseDetailDrawer` — desde 2026-09-21), `BudgetExpenseFormDrawer`, | a lista não recarrega depois de gravar | o `onChanged`/`reload` da página que a contém (as drawers não recarregam nada sozinhas) |
| **Frontend** | `components/budget/` — `BudgetExpensesDrawer` (despesas de uma rubrica: uma despesa **com fatura** abre o `InvoiceDetailDrawer` da lista de faturas, com reassociar via `BudgetItemPickerModal` e `IncidentDrawer`; só o gasto **sem fatura** abre o `BudgetExpenseDetailDrawer` — desde 2026-09-21), `BudgetExpenseFormDrawer`, | a lista diz "0 resultado(s)" com linhas, ou um cartão diz "undefined" | a página do Spring vem `{content, page:{…}}` (`VIA_DTO`) e o código lê `totalElements` no topo — usar `utils/springPage.ts` `normalizeSpringPage` |
| **Frontend** | `components/budget/` — `BudgetExpensesDrawer` (despesas de uma rubrica: uma despesa **com fatura** abre o `InvoiceDetailDrawer` da lista de faturas, com reassociar via `BudgetItemPickerModal` e `IncidentDrawer`; só o gasto **sem fatura** abre o `BudgetExpenseDetailDrawer` — desde 2026-09-21), `BudgetExpenseFormDrawer`, | a importação do Excel do orçamento falha | `BudgetExcelImportService` (procura a linha de cabeçalho "Art" ou "Rubrica") |
| **Frontend** | `components/budget/` — `BudgetExpensesDrawer` (despesas de uma rubrica: uma despesa **com fatura** abre o `InvoiceDetailDrawer` da lista de faturas, com reassociar via `BudgetItemPickerModal` e `IncidentDrawer`; só o gasto **sem fatura** abre o `BudgetExpenseDetailDrawer` — desde 2026-09-21), `BudgetExpenseFormDrawer`, | a exportação para Excel sai mal (folhas, tabelas, fórmulas, nome do ficheiro) | `BudgetExcelExportService` — o contrato é [[excel-parity]] §9 |
| **Frontend** | `components/budget/` — `BudgetExpensesDrawer` (despesas de uma rubrica: uma despesa **com fatura** abre o `InvoiceDetailDrawer` da lista de faturas, com reassociar via `BudgetItemPickerModal` e `IncidentDrawer`; só o gasto **sem fatura** abre o `BudgetExpenseDetailDrawer` — desde 2026-09-21), `BudgetExpenseFormDrawer`, | o zip da pasta da obra sai com nomes de ficheiro errados, `_2` a mais, ou sem um documento | `InvoiceDocumentsExportService` (`enterprises/service/`, endpoint `/export/zip` em `ConstructionBudgetItemController`) — o contrato é [[excel-parity]] §7; o `_EM-FALTA.txt` dentro do zip diz o que o Storage não devolveu |
| **Frontend** | `components/budget/` — `BudgetExpensesDrawer` (despesas de uma rubrica: uma despesa **com fatura** abre o `InvoiceDetailDrawer` da lista de faturas, com reassociar via `BudgetItemPickerModal` e `IncidentDrawer`; só o gasto **sem fatura** abre o `BudgetExpenseDetailDrawer` — desde 2026-09-21), `BudgetExpenseFormDrawer`, | a importação da folha "Despesas" do vault lista erros ou perguntas que não fazem sentido | `DespesasExcelImportService` (`enterprises/service/`, endpoint em `ConstructionInvoiceController` `/import-excel`) — o contrato é [[excel-parity]] §9; as frases das observações que ele lê de volta são as que o exportador gera (§4) |
| **Frontend** | `components/budget/` — `BudgetExpensesDrawer` (despesas de uma rubrica: uma despesa **com fatura** abre o `InvoiceDetailDrawer` da lista de faturas, com reassociar via `BudgetItemPickerModal` e `IncidentDrawer`; só o gasto **sem fatura** abre o `BudgetExpenseDetailDrawer` — desde 2026-09-21), `BudgetExpenseFormDrawer`, | o email de convite ou de recuperação não sai | falta um provedor predefinido **ativo** em *Definições → Provedores de email* — o erro é `EMAIL_002`/`EMAIL_003`, não `ERR_001` |
| **Frontend** | `components/budget/` — `BudgetExpensesDrawer` (despesas de uma rubrica: uma despesa **com fatura** abre o `InvoiceDetailDrawer` da lista de faturas, com reassociar via `BudgetItemPickerModal` e `IncidentDrawer`; só o gasto **sem fatura** abre o `BudgetExpenseDetailDrawer` — desde 2026-09-21), `BudgetExpenseFormDrawer`, | o link do email aponta para `localhost` | `APP_FRONTEND_URL` não está definido no ambiente — ver [[environment.md]] |
| **Frontend** | `components/budget/` — `BudgetExpensesDrawer` (despesas de uma rubrica: uma despesa **com fatura** abre o `InvoiceDetailDrawer` da lista de faturas, com reassociar via `BudgetItemPickerModal` e `IncidentDrawer`; só o gasto **sem fatura** abre o `BudgetExpenseDetailDrawer` — desde 2026-09-21), `BudgetExpenseFormDrawer`, | aceitar um convite dá erro | `InviteService#accept` — `USER_013` (desconhecido/usado/cancelado) ou `USER_012` (fora do prazo) |
| **Frontend** | `components/budget/` — `BudgetExpensesDrawer` (despesas de uma rubrica: uma despesa **com fatura** abre o `InvoiceDetailDrawer` da lista de faturas, com reassociar via `BudgetItemPickerModal` e `IncidentDrawer`; só o gasto **sem fatura** abre o `BudgetExpenseDetailDrawer` — desde 2026-09-21), `BudgetExpenseFormDrawer`, | o aviso de prazo de rubrica não aparece / aparece a dobrar | `BudgetItemDeadlineNotifier` — só `ITEM` vivas de obras não `completed/archived/deleted`, `end_date` em [hoje, hoje+N]; dedupe por `(recipient, type, entity)`, por isso adiar a data **não** reavisa |
| **Frontend** | `components/budget/` — `BudgetExpensesDrawer` (despesas de uma rubrica: uma despesa **com fatura** abre o `InvoiceDetailDrawer` da lista de faturas, com reassociar via `BudgetItemPickerModal` e `IncidentDrawer`; só o gasto **sem fatura** abre o `BudgetExpenseDetailDrawer` — desde 2026-09-21), `BudgetExpenseFormDrawer`, | recuperar a password não faz nada | `PasswordResetService` — o `204` do `forgot-password` é sempre igual, exista ou não a conta; confirmar no log se saiu email |
| **Frontend** | `components/budget/` — `BudgetExpensesDrawer` (despesas de uma rubrica: uma despesa **com fatura** abre o `InvoiceDetailDrawer` da lista de faturas, com reassociar via `BudgetItemPickerModal` e `IncidentDrawer`; só o gasto **sem fatura** abre o `BudgetExpenseDetailDrawer` — desde 2026-09-21), `BudgetExpenseFormDrawer`, | preciso de acrescentar um campo à fatura | migração → `ConstructionInvoice` → DTOs de `dto/invoice/` → `ConstructionInvoiceService` → `types/invoice.ts` → `InvoiceDetailDrawer` |
| **Frontend** | `components/budget/` — `BudgetExpensesDrawer` (despesas de uma rubrica: uma despesa **com fatura** abre o `InvoiceDetailDrawer` da lista de faturas, com reassociar via `BudgetItemPickerModal` e `IncidentDrawer`; só o gasto **sem fatura** abre o `BudgetExpenseDetailDrawer` — desde 2026-09-21), `BudgetExpenseFormDrawer`, | a fatura está `PARTIAL` e devia estar `PAID` (ou o contrário) | `paymentStatus` é derivado da soma de `invoice_payment.amount` vs. o líquido — `PaymentService` (registo/anulação) e `InvoicePaymentSummaryDTO`; uma NC ligada baixa o líquido |
| **Frontend** | `components/budget/` — `BudgetExpensesDrawer` (despesas de uma rubrica: uma despesa **com fatura** abre o `InvoiceDetailDrawer` da lista de faturas, com reassociar via `BudgetItemPickerModal` e `IncidentDrawer`; só o gasto **sem fatura** abre o `BudgetExpenseDetailDrawer` — desde 2026-09-21), `BudgetExpenseFormDrawer`, | a fatura não aparece na obra onde devia | olhar ao `scope`: `COMPANY`/`UNIDENTIFIED` não têm obra por construção (`ck_invoice_scope_enterprise`, `V26`) — muda-se com "Transferir" (`TransferInvoiceDrawer`), nunca pelo `PUT /{id}` |
| **Frontend** | `components/budget/` — `BudgetExpensesDrawer` (despesas de uma rubrica: uma despesa **com fatura** abre o `InvoiceDetailDrawer` da lista de faturas, com reassociar via `BudgetItemPickerModal` e `IncidentDrawer`; só o gasto **sem fatura** abre o `BudgetExpenseDetailDrawer` — desde 2026-09-21), `BudgetExpenseFormDrawer`, | a transferência não abriu uma inconsistência | não abre sozinha: `suggestIncident` no `InvoiceTransferResultDTO` só sugere, o `IncidentDrawer` é sempre à mão |
| **Frontend** | `components/budget/` — `BudgetExpensesDrawer` (despesas de uma rubrica: uma despesa **com fatura** abre o `InvoiceDetailDrawer` da lista de faturas, com reassociar via `BudgetItemPickerModal` e `IncidentDrawer`; só o gasto **sem fatura** abre o `BudgetExpenseDetailDrawer` — desde 2026-09-21), `BudgetExpenseFormDrawer`, | a fatura não deixa ser classificada / a repartição não fecha | `ClassifyInvoicesPage` — as linhas têm de somar o total da fatura (`V32`, `InvoiceSplitServiceTest` tem os casos) |
| **Frontend** | `components/budget/` — `BudgetExpensesDrawer` (despesas de uma rubrica: uma despesa **com fatura** abre o `InvoiceDetailDrawer` da lista de faturas, com reassociar via `BudgetItemPickerModal` e `IncidentDrawer`; só o gasto **sem fatura** abre o `BudgetExpenseDetailDrawer` — desde 2026-09-21), `BudgetExpenseFormDrawer`, 
| **Frontend** | `components/budget/` — `BudgetExpensesDrawer` (despesas de uma rubrica: uma despesa **com fatura** abre o `InvoiceDetailDrawer` da lista de faturas, com reassociar via `BudgetItemPickerModal` e `IncidentDrawer`; só o gasto **sem fatura** abre o `BudgetExpenseDetailDrawer` — desde 2026-09-21), `BudgetExpenseFormDrawer`, ---
| **Frontend** | `components/budget/` — `BudgetExpensesDrawer` (despesas de uma rubrica: uma despesa **com fatura** abre o `InvoiceDetailDrawer` da lista de faturas, com reassociar via `BudgetItemPickerModal` e `IncidentDrawer`; só o gasto **sem fatura** abre o `BudgetExpenseDetailDrawer` — desde 2026-09-21), `BudgetExpenseFormDrawer`, 
| **Frontend** | `components/budget/` — `BudgetExpensesDrawer` (despesas de uma rubrica: uma despesa **com fatura** abre o `InvoiceDetailDrawer` da lista de faturas, com reassociar via `BudgetItemPickerModal` e `IncidentDrawer`; só o gasto **sem fatura** abre o `BudgetExpenseDetailDrawer` — desde 2026-09-21), `BudgetExpenseFormDrawer`, ## Relacionado
| **Frontend** | `components/budget/` — `BudgetExpensesDrawer` (despesas de uma rubrica: uma despesa **com fatura** abre o `InvoiceDetailDrawer` da lista de faturas, com reassociar via `BudgetItemPickerModal` e `IncidentDrawer`; só o gasto **sem fatura** abre o `BudgetExpenseDetailDrawer` — desde 2026-09-21), `BudgetExpenseFormDrawer`, 
| **Frontend** | `components/budget/` — `BudgetExpensesDrawer` (despesas de uma rubrica: uma despesa **com fatura** abre o `InvoiceDetailDrawer` da lista de faturas, com reassociar via `BudgetItemPickerModal` e `IncidentDrawer`; só o gasto **sem fatura** abre o `BudgetExpenseDetailDrawer` — desde 2026-09-21), `BudgetExpenseFormDrawer`, - [[architecture.md]] — como as duas apps comunicam
| **Frontend** | `components/budget/` — `BudgetExpensesDrawer` (despesas de uma rubrica: uma despesa **com fatura** abre o `InvoiceDetailDrawer` da lista de faturas, com reassociar via `BudgetItemPickerModal` e `IncidentDrawer`; só o gasto **sem fatura** abre o `BudgetExpenseDetailDrawer` — desde 2026-09-21), `BudgetExpenseFormDrawer`, - [[api.md]] — contrato de cada endpoint
| **Frontend** | `components/budget/` — `BudgetExpensesDrawer` (despesas de uma rubrica: uma despesa **com fatura** abre o `InvoiceDetailDrawer` da lista de faturas, com reassociar via `BudgetItemPickerModal` e `IncidentDrawer`; só o gasto **sem fatura** abre o `BudgetExpenseDetailDrawer` — desde 2026-09-21), `BudgetExpenseFormDrawer`, - [[database.md]] — schema e migrações
| **Frontend** | `components/budget/` — `BudgetExpensesDrawer` (despesas de uma rubrica: uma despesa **com fatura** abre o `InvoiceDetailDrawer` da lista de faturas, com reassociar via `BudgetItemPickerModal` e `IncidentDrawer`; só o gasto **sem fatura** abre o `BudgetExpenseDetailDrawer` — desde 2026-09-21), `BudgetExpenseFormDrawer`, - [[skills/SKILLS-INDEX]] — como fazer alterações seguindo as convenções do projeto
