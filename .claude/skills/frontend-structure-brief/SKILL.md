---
name: frontend-structure-brief
description: Generates a self-contained markdown brief describing a frontend component/page/feature's current structure (component tree, props, state, data flow, styling, constraints) so it can be pasted into a separate design-focused conversation. Invoke on a referenced frontend file (Backoffice or Portal), or without one to be asked what to document. Saves to notes/design-briefs/. Use when the user wants to discuss a redesign or UI change and needs an accurate snapshot of the current implementation first.
---

Before describing any styling, also read `${CLAUDE_PROJECT_DIR}/docs/skills/references/frontend-visual-consistency.md` — it routes to the real Backoffice/Portal design tokens so the brief doesn't invent terminology.

Then read `${CLAUDE_PROJECT_DIR}/docs/skills/frontend/skill-frontend-structure-brief.md` in full and follow it step by step.

If asked to update this checklist, edit the vault file above, not this pointer.
