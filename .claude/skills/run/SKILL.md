---
name: run
description: Arranca a app Worksite localmente — backend Spring Boot (porta 8080) e Backoffice Vite (porta 5173) — em background, espera pelos marcadores de arranque nos logs, e confirma com health check por curl antes de reportar os URLs. Use quando o utilizador pedir para "correr a app", "arrancar o backend/frontend", "subir a app localmente", ou antes de testar/verificar uma feature na app real.
---

Read `${CLAUDE_PROJECT_DIR}/docs/skills/process/skill-run.md` in full and follow it step by step.

If asked to update this checklist, edit the vault file above, not this pointer.
