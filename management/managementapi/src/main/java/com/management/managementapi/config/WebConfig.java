package com.management.managementapi.config;

import org.springframework.context.annotation.Configuration;

/**
 * CORS configurado em SecurityConfig.java
 * Este arquivo é deixado vazio para evitar duplicação de configuração.
 */
@Configuration
public class WebConfig {
    // CORS já está configurado em SecurityConfig.corsConfigurationSource()
}

